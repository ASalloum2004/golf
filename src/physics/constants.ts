import * as THREE from 'three';

export const DEG2RAD = Math.PI / 180;
export const EPS = 1e-8;
export const CUP_CENTER = new THREE.Vector3(0, 0, -30);
export const CUP_R = 0.054;
export const MAX_STEP_DT = 0.05;
